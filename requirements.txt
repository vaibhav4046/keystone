duckdb>=1.0
fastapi>=0.110,<0.116
starlette>=0.40,<0.46          # FastAPI 0.11x needs <0.46; the mcp SDK wants starlette>=1.x, so run the MCP servers in a SEPARATE venv (mcp/requirements.txt)
uvicorn[standard]>=0.29
pydantic>=2.0
pytest>=8.0
httpx>=0.27
cryptography>=42.0          # RS256 verification of GitLab OIDC id_tokens (core/identity.py)
google-genai>=2.8.0          # Gemini SDK (Interactions API)
# Optional: a REAL JS/TS parser for scan-repo (core/repo_scan_ts.py). When absent the scanner
# falls back to the name-based regex pass, so the offline/zero-dep path still works; when present
# the JS/TS call graph is parsed from a true AST (accurate defs + calls, no regex approximation).
tree_sitter>=0.21
tree_sitter_javascript>=0.21
tree_sitter_typescript>=0.21

